const request = require("supertest");
const app = require("../../index");
const profile = require("../mock-data/profile.json");
const endpointUrl = "/profile/";

describe.skip(endpointUrl, () => {
  it(`GET ${endpointUrl}`, () => {});
});
